# URL base da API do Pitaco, já incluindo o context-path /api do backend.
# Lida somente no servidor — não existe equivalente NEXT_PUBLIC_*, porque nada
# no navegador fala com o backend.
PITACO_API_URL=http://localhost:8080/api
