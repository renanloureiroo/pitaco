node_modules/
.next/
out/
.git
.specify/
specs/
.vscode/
.claude/
*.md

# Testes e artefatos de teste não entram na imagem. Saem inteiros — inclusive o
# vitest.setup.ts, que lhes dá os tipos do jest-dom — porque o `next build` roda o
# type-check sobre o que sobrar, e um pedaço da suíte sem o outro não compila.
e2e/
**/__tests__/
**/__mocks__/
test-results/
playwright-report/
coverage/
playwright.config.ts
vitest.config.mts
vitest.setup.ts

# Env local pode ter segredo; a configuração vem do compose
.env
.env.*
!.env.example
